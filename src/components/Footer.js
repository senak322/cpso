import React from 'react';
import penpict from '../images/penpict.jpg';
import '../styles/Footer.css';

function Footer(params) {
    return (
        <footer className="footer" style={{backgroundImage: `url(${penpict})`}}>
        
        </footer>
    )
}

export default Footer;